<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ui="clr-namespace:XF.Material.Forms.UI;assembly=XF.Material"
             xmlns:xforms="clr-namespace:Syncfusion.ListView.XForms;assembly=Syncfusion.SfListView.XForms"
             xmlns:viewcells="clr-namespace:CheckstoresMagnusRetail.Views.ViewCells"
             x:Class="CheckstoresMagnusRetail.Views.AgregarProductoPage"
            NavigationPage.HasBackButton="False"               BackgroundColor="{StaticResource Gris}"

            
             >
        <ContentPage.ToolbarItems>

    <ToolbarItem 
                 IconImageSource="close.png"
        Clicked="close"
                 Order="Primary"
                 Priority="0" />
        
    <ToolbarItem 
                 IconImageSource="correct.png"
                 Order="Primary"
        Clicked="guardardatos"
                 Priority="1" />
</ContentPage.ToolbarItems>
    <ContentPage.Content>
      <ActivityIndicator IsRunning="True" Color="{StaticResource Key=azul}"/>
    </ContentPage.Content>
</ContentPage>
